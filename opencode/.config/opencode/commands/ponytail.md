---
description: Switch ponytail intensity level (lite/full/ultra/off)
---
Switch to ponytail {{args}} mode. If no level is specified, use full.

Lazy senior dev mode: before writing code, stop at first rung that holds:
1) does this need to exist (YAGNI), 2) stdlib, 3) native feature,
4) already-installed dependency, 5) one line, 6) minimum code that works.
