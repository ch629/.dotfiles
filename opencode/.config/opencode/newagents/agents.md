```
Ticket in
  → Ticket clarifier (Haiku) — flag ambiguities
  → Research agent (Grok) — implementation approach
  → Orchestrator (Sonnet) — scoped context per worktree
    → Test writer (Devstral) — writes tests first
    → Developer agent (DeepSeek/Devstral) — implements
    → DB reviewer (DeepSeek V4 Pro) — schema/query review
    → Migration reviewer (Haiku) — migration safety
    → Security scanner (Haiku) — diff security pass
    → Code reviewer (Sonnet) — full review
    → Completion reviewer (Haiku) — ticket criteria check
    → PR description writer (Haiku) — structured PR description
  → PR raised → your manual review
```
